# Explicit local-development/maintenance override. The normal stack has private ports.
services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"
  nodestone:
    ports:
      - "127.0.0.1:8080:8080"
