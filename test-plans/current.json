{
  "title": "Session: 2.21.0 Lodestone parser inside the bot",
  "objective": "Check 2.21.0, which moves the Lodestone parser into the bot and removes the sidecar container, while keeping everything else the same: characters, rosters and searches read exactly as before. It also brings 2.19.0 (live selectors) and 2.20.0 (TaruBot's own parser). No migration and no command change.",
  "user": [
    "Approve the restart, then run `/claim` by name and world, `/characters` and `/sync status`; everything reads as before.",
    "Optional: `/refresh force:true` as an officer, and check the roster count still matches the FC.",
    "Say if any name, world or rank looks different."
  ],
  "assistant": [
    "Deploy 2.21.0 to DevBot and production with the owner's go-ahead (restarts with --remove-orphans, no migration).",
    "Check readiness's lodestone object (selectors current), that no sidecar container remains, and one roster and one profile refresh in the logs.",
    "Record the rollouts in docs/DEV_GUILD.md and docs/VERIFICATION.md."
  ],
  "bot": [
    "Run compiled 2.21.0 on schema 008 with the Lodestone parser in process.",
    "Follow the selectors' upstream HEAD live and parse every page exactly as before.",
    "Post this responsibility-separated session plan in #chat."
  ]
}
