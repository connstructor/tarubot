{
  "title": "Session: Drizzle persistence regression",
  "objective": "Validate version 2.9.0 typed persistence against existing PostgreSQL state, exact ledger values, role and guest policy, and durable work recovery before resuming lobby security setup.",
  "user": [
    "Review the Drizzle PR after CI and required CodeQL security/code-quality checks pass; merge to publish its images.",
    "Pull the published 2.9.0 images with the DevBot Compose overlay and keep the existing isolated tarubot_dev database.",
    "Run `/version`, `/config validate`, `/characters`, `/guest status`, and `/sync status`; verify retained configuration, links, and guest decisions.",
    "Run `/refresh` and inspect its run with `/sync status`; confirm role/nickname reconciliation and complete child-work accounting.",
    "Check `/ledger balance` and `/ledger history`, then report any changed values or blocked work for investigation."
  ],
  "assistant": [
    "Verify schema mappings, exact IDs/money/JSON, atomic policy/audit/outbox rollback, and concurrent lease/generation fencing against PostgreSQL.",
    "Run source quality and full tests with both the supplied dump and generated CI fixture; inspect PR checks and published image metadata.",
    "Record the development smoke results, then adapt the saved lobby/security feature to the verified ORM foundation."
  ],
  "bot": [
    "Run compiled version 2.9.0 with typed Drizzle queries and verify the existing SQL migration checksum at startup.",
    "Retain links, grants, nicknames, exact ledger history, and queued work in PostgreSQL across container replacement.",
    "Reconcile current roles and resume durable work after startup; expose capability and delivery status.",
    "Post this responsibility-separated session plan in the configured development channel."
  ]
}
