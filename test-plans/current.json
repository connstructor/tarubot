{
  "title": "Session: 2.24.2 code-scanning fixes",
  "objective": "2.24.2 closes the two CodeQL alerts in the Lodestone parser: the worker now ignores messages that don't come from the bot itself, and a null check reads correctly. Parsing is unchanged. No migration and no command change.",
  "user": [
    "Run /claim or /characters once: character and FC names read as before.",
    "Check the repository's Security tab: both alerts should show as fixed after the merge.",
    "Confirm the 'TaruBot backups' check after the first scheduled run (04:30 UTC)."
  ],
  "assistant": [
    "Deploy 2.24.2 to DevBot and production with the owner's go-ahead (restarts, no migration).",
    "Check the next day's scheduled backup in ~/tarubot-backup.log and the bucket.",
    "Record the rollouts in docs/DEV_GUILD.md and docs/VERIFICATION.md."
  ],
  "bot": [
    "Run compiled 2.24.2 on schema 008 with the heartbeat on in production.",
    "Parse Lodestone pages in workers that accept only the bot's own messages.",
    "Post this responsibility-separated session plan in #chat."
  ]
}
