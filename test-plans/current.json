{
  "title": "Session: 2.19.0 live Lodestone selectors",
  "objective": "Check that the sidecar keeps xivapi/lodestone-css-selectors at its latest commit by itself: 2.19.0 loads selectors at runtime, activates each new upstream HEAD after validating it, and shows the live revision in /health and issue reports. No migration and no command change.",
  "user": [
    "Approve the restart, then run `/claim` or `/refresh` as usual; answers read as before.",
    "Optional: run `/issue` and check the Lodestone section shows the selectors commit.",
    "Say if anything parses differently."
  ],
  "assistant": [
    "Restart DevBot and production on 2.19.0 with the owner's go-ahead (no migration, no registration).",
    "Check the sidecar's /health: selectors at the latest upstream commit and the upstream status current.",
    "Record both restarts in docs/DEV_GUILD.md and docs/VERIFICATION.md."
  ],
  "bot": [
    "Run compiled 2.19.0 on schema 008.",
    "Parse with the live selector set, following upstream HEAD every 15 minutes.",
    "Post this responsibility-separated session plan in #chat."
  ]
}
