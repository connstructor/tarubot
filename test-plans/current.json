{
  "title": "Session: 2.24.1 unresolved failures only",
  "objective": "2.24.1 makes /sync status list a failed job only until the same work later succeeds, so the first night's resolved Lodestone failures stop showing. With 2.24.0, the host also starts its daily encrypted backup. No migration and no command change.",
  "user": [
    "Run /sync status as an officer: the old profile failures are gone, and runs and pending work read as before.",
    "Say if any failure you'd expect to see is missing.",
    "Check the 'TaruBot backups' check after the first scheduled run (04:30 UTC)."
  ],
  "assistant": [
    "Deploy 2.24.1 to DevBot and production with the owner's go-ahead (restarts, no migration).",
    "Install the backup crontab line on the host and confirm its first scheduled run.",
    "Record the rollouts in docs/DEV_GUILD.md and docs/VERIFICATION.md."
  ],
  "bot": [
    "Run compiled 2.24.1 on schema 008, heartbeat on in production.",
    "List only unresolved failures in /sync status.",
    "Post this responsibility-separated session plan in #chat."
  ]
}
