{
  "title": "Session: 2.30.1 cleanup patch",
  "objective": "2.30.1 (#48) catches up records and deploy docs after 2.30.0, marks the agent rule confirmed, drops the DigitalOcean tool-guard paths, gives the Claude review a read-only tool list, pins deploy.sh's umask 077 with tests, and re-slots the public roadmap (v3 stabilization and web baseline, v4 member web app). No bot behavior change, no migration.",
  "user": [
    "Approve the 2.30.1 Deploy production run; expect deployed (a restart) and one Pushover message.",
    "Owed from earlier releases, run or waive: 2.25.0 changelog checks, 2.28.0 /suggest checks, 2.29.0 status posts (/officer revoke and reset; /guest grant, revoke and reset).",
    "Check the docs site's search, 404 page and edit links; confirm Public Bot is off and both apps' portal settings match the docs.",
    "Optional on the host: chmod -R go-w ~/tarubot once."
  ],
  "assistant": [
    "Update DevBot to 2.30.1 with the owner's go-ahead (no migration) and read back the commands.",
    "Record the 2.30.1 run in docs/VERIFICATION.md; follow the agent rule: never approve, reject, cancel or re-run a deployment."
  ],
  "bot": [
    "Run 2.30.1 on schema 010; nothing changes for members.",
    "Post this responsibility-separated session plan in #chat."
  ]
}
