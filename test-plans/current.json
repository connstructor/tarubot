{
  "title": "Session: 2.30.3 hardened containers",
  "objective": "2.30.3 (#51) runs the bot with a read-only root, no Linux capabilities and no-new-privileges (the backup job too, with a 1 MB /tmp). Compose-only; no migration; nothing changes for members.",
  "user": [
    "Go-ahead for DevBot's rehearsal (restart with the new Compose settings), then approve the 2.30.3 Deploy production run only after it passes.",
    "After the deploy: /version shows 2.30.3; the next nightly backup check is green."
  ],
  "assistant": [
    "Rehearse on DevBot: settings in force (docker inspect), readiness, no EROFS/EACCES in logs, the #chat plan post, /config validate, /refresh force:true, commands.js list.",
    "Record the rehearsal and the production run in docs/VERIFICATION.md."
  ],
  "bot": [
    "Run 2.30.3 on schema 010 with a read-only root; nothing changes for members.",
    "Post this responsibility-separated session plan in #chat."
  ]
}
