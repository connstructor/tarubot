{
  "title": "Session: 2.20.0 TaruBot's own Lodestone parser",
  "objective": "Check 2.20.0, which replaces Nodestone with TaruBot's own parser while keeping everything else the same: characters, rosters and searches read exactly as before (identical output was verified on live pages). No migration and no command change.",
  "user": [
    "Approve the restart, then run `/claim` by name and world, `/characters` and `/sync status`; everything reads as before.",
    "Optional: `/refresh force:true` as an officer, and check the roster count still matches the FC.",
    "Say if any name, world or rank looks different."
  ],
  "assistant": [
    "Deploy 2.19.0 and 2.20.0 to DevBot and production with the owner's go-ahead (restarts, no migration).",
    "Check the sidecar's /health (selectors current), one roster acquisition and one profile refresh in the logs.",
    "Record both rollouts in docs/DEV_GUILD.md and docs/VERIFICATION.md."
  ],
  "bot": [
    "Run compiled 2.20.0 on schema 008 with the first-party Lodestone parser.",
    "Follow the selectors' upstream HEAD live and parse every page exactly as before.",
    "Post this responsibility-separated session plan in #chat."
  ]
}
