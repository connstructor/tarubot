{
  "title": "Session: 2.24.0 daily encrypted backups",
  "objective": "2.24.0 adds a daily encrypted database dump from the production host to Linode Object Storage, with its own healthchecks.io check. The bot itself doesn't change: DevBot and production keep running, and deploying is a pull plus a crontab line on the host.",
  "user": [
    "Keep the 'TaruBot backups' check at period 1 day, grace 3 hours, with Pushover for down.",
    "After the first scheduled run (04:30 UTC), confirm the check shows the dump size.",
    "Optional: attach a Linode Cloud Firewall to the tarubot Linode (inbound SSH and ICMP only)."
  ],
  "assistant": [
    "After the merge, pull on the host and install the backup schedule (no bot restart).",
    "Check the first scheduled run in ~/tarubot-backup.log and the bucket.",
    "Prepare 2.25.0: deploys over SSH from GitHub Actions."
  ],
  "bot": [
    "Keep running 2.22.0 on schema 008 with the heartbeat on in production.",
    "Nothing changes in the running bot for this session.",
    "Post this responsibility-separated session plan in #chat on DevBot's next restart."
  ]
}
